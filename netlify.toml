[build]
publish = "frontend"
functions = "netlify/functions"

[dev]
publish = "frontend"
functions = "netlify/functions"
port = 8888

[functions]
node_bundler = "esbuild"
external_node_modules = ["express", "firebase-admin", "cors", "dotenv"]
